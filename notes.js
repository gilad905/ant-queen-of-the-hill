/*

   if is queen:
      if has 2 friends around:
         move to corner between them
      else:
         if has 1 friend around:
            if has food:
               produce 1 friend, type 2, 2 squares near existing friend
            else:
               move somewhere
         else:
            if has food:
               produce 1 friend, type 1, at square 1
            else:
               move somewhere
   else:
      if type 1:
         ...

*/
